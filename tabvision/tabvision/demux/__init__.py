"""Demuxing — see SPEC.md §3.3, §8.

Public entrypoint: ``demux(video_path) -> DemuxResult``.

Wraps ffmpeg to split a video file into a wav buffer + frame iterator.

Implementation deferred to Phase 1.
"""
