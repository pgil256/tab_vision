"""Local-only personal training data from the user's own recordings.

Everything in this package operates under the SPEC §1.5 carve-out
(2026-08-02, widened same day for gold-tab sessions): the user's own
recordings and user-supplied reference tabs may build **local** artifacts —
the personal position prior and a video-training corpus — that are never
shipped, never registered as defaults, never used in eval corpora or
published figures.
"""
