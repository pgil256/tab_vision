"""Keypoint fretboard detection — Phase 3 fallback deliverable. Stub.

Fine-tuned YOLOv8-pose with 14 keypoints (nut + fret-line endpoints at
frets 1, 3, 5, 7, 9, 12). Used only if the geometric path fails on a
definable failure class.
"""
