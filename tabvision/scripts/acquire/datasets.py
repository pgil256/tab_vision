"""Dataset acquisition skeleton — see SPEC.md §6.2.

Each dataset has its own download + checksum logic. Idempotent. Skips if
already present at ``$TABVISION_DATA_ROOT``.

Phase 0 stub. Filled in as each dataset is needed (Phase 1.5+, Phase 7).
"""

from __future__ import annotations


def main() -> int:
    raise NotImplementedError("Phase 1.5+ deliverable")


if __name__ == "__main__":
    raise SystemExit(main())
